# ADR-0026: Budget Disc Metadata in Planning

## Decision

The planner budgets payloads, encrypted metadata, proofs, README content, and ISO overhead when deciding whether an image fits.

## Reason

A planned image that ignores mandatory recovery metadata is not a valid physical artifact.
