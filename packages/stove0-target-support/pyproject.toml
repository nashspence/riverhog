[build-system]
requires = ["hatchling>=1.32.0"]
build-backend = "hatchling.build"

[project]
name = "stove0-target-support"
version = "0.1.0"
description = "Hardware-neutral target protocol, runtime, and conformance support for stove0."
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "Riverhog v1 component. See the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }
dependencies = [
  "http-api-contracts>=0.1,<0.2",
  "jsonschema>=4.25,<5.0",
  "pydantic>=2.7,<3.0",
  "riverhog-api-client>=0.1,<0.2",
  "riverhog-protocol>=0.1,<0.2",
  "riverhog-transform-sdk>=0.1,<0.2",
  "stove0-target-client>=0.1,<0.2",
  "stove0-target-protocol>=0.1,<0.2",
]

[project.scripts]
stove0-target-conformance = "stove0_target_support.conformance:main"
stove0-target-schemas = "stove0_target_support.schemas:main"

[tool.uv.sources]
http-api-contracts = { workspace = true }
riverhog-api-client = { workspace = true }
riverhog-protocol = { workspace = true }
riverhog-transform-sdk = { workspace = true }
stove0-target-client = { workspace = true }
stove0-target-protocol = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/stove0_target_support"]
