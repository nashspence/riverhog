[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "time-formats"
version = "0.1.0"
description = "UTC timestamp and operator duration formats."
requires-python = ">=3.12"
dependencies = ["pydantic>=2.12,<3.0"]
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "UTC timestamp and operator duration formats.\n\nSee the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }

[tool.riverhog.public-api]
modules = ["time_formats"]

[tool.hatch.build.targets.wheel]
packages = ["src/time_formats"]
