[build-system]
requires = ["hatchling>=1.32.0"]
build-backend = "hatchling.build"

[project]
name = "riverhog-storage-adapter-s3-support"
version = "0.1.0"
description = "Optional nonnormative S3 support for Riverhog storage references."
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "Optional nonnormative Riverhog v1 reference component. See the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }
dependencies = [
  "boto3>=1.35,<2.0",
  "botocore>=1.35,<2.0",
  "riverhog-storage-adapter-protocol>=0.1,<0.2",
  "time-formats>=0.1,<0.2",
]

[tool.uv.sources]
riverhog-storage-adapter-protocol = { workspace = true }
time-formats = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/riverhog_storage_adapter_s3_support"]
