# NON-AUTHORITATIVE EXTERNAL REFERENCE

Owning issue: https://github.com/nashspence/riverhog/issues/895
Convention: https://github.com/nashspence/riverhog/issues/903
Audited base: main @ d4f944ca350810dfb14377ffd9cf77dcd06468f0
Reference branch: reference/external/895-closure-audit-html

Produced by: OpenAI ChatGPT
Model: GPT-6 Astra Pro (identity exposed in this session)
Configuration/effort: no separate configuration value exposed; not inferred
Requested by: Riverhog maintainer, relaying the Riverhog integration agent's request
Published by: the same external agent through the GitHub connector

## Purpose and contents

A minimal working #895 reference for field/extent ownership, deterministic Closure–Audit binding and shared static HTML. Includes a selected source-value fixture, legacy-shape adapter, exact partition/reconstruction checks, literal-safe rendering, Python-kind and compatibility inventory facts (#888/#893), and focused test/browser runners. See [the implementation reference](895/README.md).

This branch is externally produced reference material. Its creation, tests, publication or request by an authorized repository agent does not establish or extend an accepted design, contract, release requirement or authorization to integrate these changes. The owning issue, maintainer decisions and normal integration rail remain authoritative. Reconcile against then-current main; do not apply mechanically.

The exact GitHub reference commit recorded in the owning issue is the durable handoff identity. The publisher reconstructs a tree on the audited base through GitHub Git objects and verifies every added blob against the supplied local bytes. There is no producer Git commit SHA being represented as imported verbatim. No force-push, main change, release change, PR or closing keyword is part of this handoff.

## Validation actually performed

- Python 3.13.5: 27 focused unittest cases pass using byte-verified exact source modules; source compiles. Cases include explicit field roles, exact restoration, deterministic generation, stale/ambiguous targets, references and recursion, unsafe integer envelopes, unsupported roles, evidence isolation, Python member ownership, visible literals and real HTML links.
- Five inspected source values reproduce their existing generated canonical hashes. The other fixture declarations were transcribed from the exact base release/extent source and are labelled as a selected fixture, not a regenerated whole closure.
- Chromium 144.0.7559.96 with Playwright: actual visible difficult literals, comparison facts, contract-body invariance under the mode display function, no-JavaScript base, and layout at 1280/640 CSS-pixel widths pass. No script injection or JavaScript errors observed. Representative compatibility and regex/audit captures visually inspected.
- A deterministic selected preview is generated with 15 records across 4 interface types (5 authority/interface inventories). Its manifest checks output bytes without hashing itself. `validation.json` records the preview identities and executed-check scope.

## Validation not performed / limitations

The full repository and large checked Closure could not be downloaded into this working container: external DNS/network access was unavailable. Connector reads supplied the inspected source and exact values. For final execution, the complete canonical JSON module from the base and both RFC8785 v0.1.4 modules were copied locally and verified byte-for-byte against their Git blob SHAs before rerunning all checks. They are not modifications or substitute helper implementations. These existing dependency sources are **not vendored** in the reference branch. This remains a selected-module environment, not the prepared full Riverhog workspace; rerun there before integration.

Verified dependency blobs:
- Riverhog canonical module: `8c25c884c893134d57cfb1db7795bcc5867240d3`.
- RFC8785 v0.1.4 `_impl.py`: `3137d3326b98938affadb1be711ee411eb2ab86e`.
- RFC8785 v0.1.4 `__init__.py`: `5a1f9d919643fa3bcaa0999ea66d9c535568c42a`.

The browser allowed about:blank/set_content but blocked navigation to the intercepted static origin and local-file route. The executed browser mode inlined the exact generated CSS/JS into the HTML DOM. URL-driven checkbox/history/deep-link transitions, live Pages/base-path behavior and hosted artifacts were **not** tested. The supplied runner exposes those checks for a capable environment. A 640-pixel layout check is not a claimed 200% human zoom pass.

No `make lint`, full `make unit`, `make dist-smoke`, `make build`, full-closure generation/semantic comparison, release packaging, runtime behavioral qualification, documentation work or maintainer human acceptance was performed. GitHub status/check observations and native Development-link availability are recorded with the actual published SHA in the owning-issue comment, not presumed here.

Only explicitly supported field/extent forms are implemented. General principles, segmented policies, full policy applicability and all-type migration remain with the integration agent. Unknown meaning fails visibly rather than being silently contractualized or discarded. Synthetic witness/member cases are tests, not repository evidence. Existing normative files and generated baselines remain untouched.

The publication interface exposes no native issue–branch Development linking action. A Markdown link is not a substitute: the owning-issue handoff explicitly records this limitation so a capable publisher may add the native relationship without changing the reference commit.
