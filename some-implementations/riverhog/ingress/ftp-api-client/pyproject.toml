[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "a-riverhog-ftp-spool-client"
version = "0.1.0"
description = "Client for the Riverhog FTP upload spool."
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "Client for the Riverhog FTP upload spool.\n\nSee the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }
dependencies = [
  "http-api-contracts>=0.1,<0.2",
  "lifecycle-events>=0.1,<0.2",
  "httpx>=0.27,<1.0",
  "pydantic>=2.7,<3.0",
]

[tool.uv.sources]
http-api-contracts = { workspace = true }
lifecycle-events = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/a_riverhog_ftp_spool_client"]
