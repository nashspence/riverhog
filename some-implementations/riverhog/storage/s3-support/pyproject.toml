[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "a-riverhog-s3-store-lib"
version = "0.1.0"
description = "S3 store library for Riverhog storage adapters."
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "S3 store library for Riverhog storage adapters.\n\nSee the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }
dependencies = [
  "boto3>=1.35,<2.0",
  "botocore>=1.35,<2.0",
  "riverhog-storage-adapter-protocol>=0.1,<0.2",
  "time-formats>=0.1,<0.2",
]

[tool.uv.sources]
riverhog-storage-adapter-protocol = { workspace = true }
time-formats = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/a_riverhog_s3_store_lib"]
