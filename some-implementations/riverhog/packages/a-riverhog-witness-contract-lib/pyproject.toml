[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "a-riverhog-witness-contract-lib"
version = "0.1.0"
description = "Shared collection witness statement for supplied Riverhog witness applications."
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = { text = "Shared collection witness statement for supplied Riverhog witness applications.\n\nSee the project URL for documentation and releases.", content-type = "text/markdown" }
authors = [{ name = "Nash Spence" }]
maintainers = [{ name = "Nash Spence" }]
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
urls = { Documentation = "https://nashspence.github.io/riverhog/v1/", Issues = "https://github.com/nashspence/riverhog/issues", Repository = "https://github.com/nashspence/riverhog" }
dependencies = [
  "riverhog-canonical-json>=0.1,<0.2",
  "riverhog-protocol>=0.1,<0.2",
]

[tool.uv.sources]
riverhog-canonical-json = { workspace = true }
riverhog-protocol = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/a_riverhog_witness_contract_lib"]
